@~/.config/claude/_private/CLAUDE-common.md

## client プロファイル

担当する取引先 1 社の案件ディレクトリで有効になる。対象ディレクトリの一覧は `_private/CLAUDE-common.md` にある。

この配下でも gws と gcloud は `hucom-system` のまま使う。切り替えは direnv が行うため、`gws-profile` を手で呼ぶ必要はない。
