{{- $client := dict -}}
{{- if hasKey . "clientSecretsLedger" }}{{ $client = .clientSecretsLedger }}{{ end -}}
{{- $ledger := merge (dict) .secretsLedger $client -}}
# 生成物: 編集しない。正本は client-dotfiles の秘密台帳（chezmoi apply 時に 1Password から注入）。
# 形式: 生の KEY=VALUE（シェル引用なし）。headersHelper・keywrap ラッパー・mise タスクが行単位で読む。
{{- if hasKey $ledger "hucom" }}
{{- range $svc, $s := index $ledger "hucom" }}
{{- range $s.vars }}
{{ .name }}={{ onepasswordRead .op }}
{{- end }}
{{- end }}
{{- end }}
